name: 15m update

on:
  schedule:
    - cron: '55/15 * * * *'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2

      - name: Use Node.js 18.x
        uses: actions/setup-node@v2
        with:
          node-version: '18.x'

      - name: Install dependencies
        run: npm install

      - name: Run sources
        env:
          NODE_OPTIONS: --max-old-space-size=8192 --experimental-json-modules
        run: |
          echo "Starting sources update..."
          echo "Checking connection files..."
          cat .evidence/template/sources/trav/connection.yaml
          cat .evidence/template/sources/trav/connection.options.yaml
          echo "Running sources..."
          npm run sources || exit 1
          echo "Sources update completed successfully"
          echo "Checking generated files..."
          ls -la .evidence/template/sources/trav/
          ls -la .evidence/meta/query-cache/
          cat .evidence/meta/query-cache/hashes.json

      - name: Build project
        env:
          NODE_OPTIONS: --max-old-space-size=8192 --experimental-json-modules
        run: |
          echo "Starting build process..."
          echo "Verifying sources directory..."
          ls -la .evidence/template/sources/trav/
          ls -la .evidence/meta/query-cache/
          npm run build || exit 1
          echo "Build completed successfully"

      - name: Deploy to FTP
        uses: SamKirkland/FTP-Deploy-Action@4.3.3
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: build/
          server-dir: /public_html/spy.sander.wtf/
